; the value of this define will come from the Makefile
; !resources_directory

; add below defines for every resource in !resources_directory, for example:
;
; !graphics = "!resources_directory/graphics.bin"
